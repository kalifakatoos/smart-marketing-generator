# مسوّق المنتجات الذكي 🚀

تطبيق ويب ذكي لتوليد المحتوى التسويقي للمنتجات باستخدام الذكاء الاصطناعي (Google Gemini API) مع تكامل آمن عبر Supabase Edge Functions

## المميزات الرئيسية ✨

- 🤖 **تحليل الصور بالذكاء الاصطناعي**: يرفع صور المنتجات ويحللها تلقائياً
- 📝 **توليد المحتوى التسويقي**: ينشئ نصاً تسويقياً جذاباً ووصفاً تفصيلياً
- 🏷️ **توليد الهاشتاغات**: يقترح هاشتاغات مناسبة لمنصات التواصل الاجتماعي
- 🎨 **موجه إنشاء الصور**: ينتج prompts مخصصة لصور AI احترافية
- 📤 **تصدير البيانات**: إمكانية تصدير المحتوى بصيغة JSON
- 🔗 **تكامل Webhook**: ربط مع منصات الأتمتة مثل n8n
- 🔐 **أمان متقدم**: API Key محمي في Supabase Edge Functions
- 🌙 **تصميم داكن**: واجهة مستخدم عصرية مع دعم RTL الكامل
- 📱 **تصميم متجاوب**: يعمل بشكل مثالي على جميع الأجهزة

## التقنيات المستخدمة 🛠️

- **React 18** مع TypeScript
- **Tailwind CSS** للتصميم
- **Lucide React** للأيقونات
- **Vite** لبناء المشروع
- **Supabase Edge Functions** للباك إند الآمن
- **Google Gemini API** للذكاء الاصطناعي

## البنية المعمارية 🏗️

```
Frontend (React + TypeScript)
    ↓ HTTPS Request
Supabase Edge Function
    ↓ Server-side Processing
Google Gemini API
    ↓ AI Response
Edge Function
    ↓ Formatted Results
Frontend Display
```

## متطلبات التشغيل 📋

- Node.js 16+ 
- npm أو pnpm
- حساب Supabase (مجاني)
- Google Gemini API Key

## طريقة الإعداد والتشغيل 🚀

### 1. تحميل المستودع
```bash
git clone [repository-url]
cd smart-marketing-generator
```

### 2. تثبيت المتطلبات
```bash
npm install
# أو
pnpm install
```

### 3. إعداد Supabase

#### أ) إنشاء مشروع Supabase
1. انتقل إلى [Supabase Dashboard](https://app.supabase.com)
2. أنشئ مشروع جديد (أو استخدم مشروع موجود)
3. احصل على:
   - Project URL
   - Anon (public) key

#### ب) نشر Edge Function
```bash
# تثبيت Supabase CLI
npm install -g supabase

# تسجيل الدخول
supabase login

# ربط المشروع
supabase link --project-ref your-project-ref

# نشر Edge Function
supabase functions deploy generate-marketing-content

# إضافة Gemini API Key
supabase secrets set GEMINI_API_KEY=AIzaSyA0pKbwyWcpXOJMbDhMMKVv1LB50F_TjyI
```

### 4. إعداد ملف البيئة
انسخ الملف `.env.example` إلى `.env` وضع معلومات Supabase:

```bash
cp .env.example .env
```

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# إعدادات التطبيق
VITE_APP_NAME=مسوّق المنتجات الذكي
VITE_APP_VERSION=2.0.0

# إعدادات الملفات
VITE_MAX_FILE_SIZE=5242880
VITE_MAX_IMAGES=10
```

### 5. تشغيل المشروع للتطوير
```bash
npm run dev
# أو
pnpm dev
```

### 6. بناء المشروع للإنتاج
```bash
npm run build
# أو
pnpm build
```

## التشغيل عبر Docker 🐳

### المتطلبات
- تثبيت Docker Desktop (Windows/Mac) أو Docker Engine (Linux)
- ملف `.env` مكتمل بالقيم المطلوبة (راجع `.env.example`)

### تشغيل بيئة التطوير داخل Docker
```bash
# يشغّل خادم Vite داخل حاوية مع إعادة تحميل تلقائي
docker compose up dev

# افتح المتصفح على: http://localhost:5173
```

ملاحظات:
- يتم تمرير متغيرات `VITE_*` من `.env` إلى الحاوية.
- على Windows، تم تفعيل `CHOKIDAR_USEPOLLING=1` لتحسين تتبع الملفات داخل الحاوية.

### تشغيل نسخة إنتاجية محلية (Static Build)
```bash
# يبني صورة الإنتاج ويشغّلها عبر nginx
docker compose up --build web

# افتح المتصفح على: http://localhost:4000
```

آلية العمل:
- يتم بناء المشروع داخل صورة Node باستخدام `pnpm`.
- يتم تمرير متغيرات `VITE_*` أثناء مرحلة البناء لضمان دمجها في حزمة الإنتاج.
- يتم تقديم الملفات الناتجة عبر `nginx` مع تهيئة SPA (fallback إلى `index.html`).

### تخصيصات مفيدة
- تعديل منفذ الاستضافة: غيّر خرائط المنافذ في `docker-compose.yml`.
- تغيير إعدادات الإنتاج: مرّر `BUILD_MODE=prod` كـ build arg (القيمة الافتراضية مفعّلة).
- مراقبة السجلّات: استخدم `docker compose logs -f web` أو `docker compose logs -f dev`.


## ملاحظات الأمان 🔐

- ✅ **API Key محمي**: لا يظهر في Frontend أبداً
- ✅ **Server-side Processing**: معالجة الطلبات في Supabase
- ✅ **HTTPS Only**: جميع الاتصالات مشفرة
- ✅ **Authorization**: كل طلب يتطلب مصادقة

للمزيد من التفاصيل التقنية، راجع [دليل التكامل](./INTEGRATION_GUIDE.md)

## طريقة الاستخدام 📖

### 1. رفع الصور
- اسحب الصور إلى المنطقة المخصصة أو انقر للاختيار
- يدعم التطبيق صيغ JPG, PNG, GIF حتى 5 ميجابايت لكل صورة
- يمكن رفع عدة صور في نفس الوقت

### 2. توليد المحتوى
- انقر على زر "أنشئ المحتوى"
- انتظر حتى ينتهي الذكاء الاصطناعي من التحليل
- ستظهر النتائج مقسمة إلى أقسام منظمة

### 3. النتائج المتوقعة 📊

#### أ) اسم المنتج
- عنوان جذاب وجاهز للتسويق

#### ب) النسخ التسويقي
- نص قصير ومؤثر للوسائل الاجتماعية

#### ج) الوصف التفصيلي
- وصف شامل للمنتج (4-6 جمل)

#### د) الميزات الرئيسية
- قائمة ب3-5 ميزات أساسية

#### هـ) الهاشتاغات
- 5-7 هاشتاغات مناسبة

#### و) موجه إنشاء الصورة
- prompt بالإنجليزية لصور AI احترافية

### 4. نسخ النصوص
- انقر على زر "نسخ" بجانب أي قسم لنسخه للحافظة

### 5. تصدير البيانات
- انقر على "تصدير المحتوى إلى JSON" لحفظ النتائج

### 6. إرسال إلى Webhook
- أدخل رابط الـ webhook (مثل n8n)
- انقر على "إرسال إلى Webhook" لنقل البيانات

## هيكل البيانات المرسلة إلى Webhook 📡

```json
{
  "content": {
    "productName": "اسم المنتج",
    "marketingCopy": "النص التسويقي",
    "productDescription": "الوصف التفصيلي",
    "keyFeatures": ["الميزة 1", "الميزة 2", "الميزة 3"],
    "hashtags": ["#هاشتاغ1", "#هاشتاغ2", "#هاشتاغ3"],
    "imagePrompt": "English prompt for image generation"
  },
  "images": [
    {
      "base64": "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD...",
      "fileName": "product.jpg",
      "mimeType": "image/jpeg"
    }
  ],
  "timestamp": "2025-10-29T23:01:08.000Z"
}
```

## نصائح للاستخدام الأمثل 💡

1. **جودة الصور**: استخدم صوراً واضحة وعالية الجودة للحصول على نتائج أفضل
2. **تنوع الصور**: ارفع صوراً من زوايا مختلفة لإنتاج محتوى أكثر شمولية
3. **صور منتج واحدة**: حتى صورة واحدة تكفي لتوليد محتوى ممتاز
4. **فحص النتائج**: راجع وعدل المحتوى قبل استخدامه في التسويق

## استكشاف الأخطاء وحلها 🔧

### خطأ "Supabase configuration missing"
- تأكد من صحة SUPABASE_URL في ملف `.env`
- تأكد من صحة SUPABASE_ANON_KEY في ملف `.env`
- أعد تشغيل التطبيق بعد تحديث `.env`

### خطأ "Gemini API key غير متوفر"
- تأكد من إضافة API Key في Supabase Secrets
- تشغيل: `supabase secrets set GEMINI_API_KEY=your-key`
- تحقق من صحة الـ API Key

### خطأ 500 من Edge Function
- راجع Logs في Supabase Dashboard
- تأكد من نشر Edge Function بشكل صحيح
- تحقق من اتصال Gemini API

### خطأ في رفع الصور
- تأكد من أن حجم الصورة أقل من 5 ميجابايت
- تأكد من أن صيغة الصورة مدعومة (JPG, PNG, GIF)
- جرب تحديث المتصفح أو مسح الذاكرة المؤقتة

### خطأ في توليد المحتوى
- تأكد من اتصال الإنترنت
- تحقق من صحة Supabase configuration
- انتظر بضع دقائق قبل المحاولة مرة أخرى
- راجع Console في المتصفح للأخطاء

### مشاكل في Webhook
- تأكد من صحة رابط الـ webhook
- تحقق من أن الـ webhook يستقبل JSON
- راجع سجلات الـ webhook للتفاصيل

## المساهمة 🤝

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء branch جديد
3. تطبيق التعديلات
4. إرسال Pull Request

## الدعم والمساعدة 📞

للدعم أو الاستفسارات:
- فتح issue في المستودع
- مراجعة الوثائق
- البحث في المشاكل المعروفة

## الترخيص 📄

هذا المشروع مرخص تحت رخصة MIT. انظر ملف LICENSE للتفاصيل.

---

**ملاحظة مهمة**: هذا التطبيق يتطلب اتصالاً بالإنترنت ويعتمد على خدمات Google Cloud. تأكد من احترام شروط الاستخدام وإرشادات الخصوصية.
